<%= simple_form_for(@place) do |f| %>
  <%= f.input :address %>
  <%= f.input :beds %>
  <%= f.input :price %>
  <%= f.input :photos, as: :file %>
  <%= f.submit "Submit",  class: "btn btn-primary"%>
<% end %>